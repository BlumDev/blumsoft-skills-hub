# Review codex 2026-07-14

- Modell: gpt-5.6-sol (read-only via ai-router Gate)
- Scope: dieses Repo (blumsoft-skills-hub)
- Status: roh, noch nicht validiert. Triage -> docs/backlog.md ## Offen (IDs 20260714-*).

## 1. Findings

scripts/project/bootstrap-project.ps1:55 - high - Das String-Array wird positionell statt benannt gesplattet, wodurch `-WorkspaceRoot` als Profil und der Zielpfad als Bundle-ID gebunden werden, Bootstrap kopiert zunächst Dateien und scheitert danach zuverlässig.

scripts/skills/vendor-import.ps1:44 - high - Bereits vorhandene Skills werden nicht aktualisiert, anschließend aber mit dem aktuellen Repository-HEAD in `UPSTREAM.md` und `vendor-lock.json` markiert, wodurch die Provenienz nachweislich falsch wird.

scripts/skills/vendor-import.ps1:34 - high - Der Import verwendet den gesperrten Commit nicht und prüft `$LASTEXITCODE` von `py` nicht, dadurch können unreviewter HEAD-Code oder Teilimporte als erfolgreich übernommen werden.

scripts/skills/sync.ps1:66 - high - Registry-Namen werden ungeprüft mit `-Path` an rekursives `Remove-Item` übergeben, sodass `..` oder Wildcards das Zielverzeichnis verlassen oder mehrere installierte Skills löschen können.

scripts/skills/sync.ps1:25 - medium - Aus Profilen werden nur `bundle_ids` und `include_extended` gelesen, `default_targets` bleibt wirkungslos und der Standardlauf synchronisiert zusätzlich nach Claude und in das aktuelle Repository.

scripts/project/bootstrap-project.ps1:30 - medium - `-DryRun` schützt die Template-Kopien nicht, daher verändert bereits ein vermeintlicher Probelauf das Zielprojekt.

scripts/skills/sync.ps1:66 - medium - Das vorhandene Ziel wird vor erfolgreichem Kopieren gelöscht, ein Lesefehler oder Abbruch hinterlässt einen fehlenden beziehungsweise teilweise installierten Skill.

skills/custom/web/scripts/with_server.py:72 - medium - `stdout` und `stderr` werden in Pipes umgeleitet, aber nie konsumiert, wodurch ausgabestarke Server nach gefülltem Pipe-Puffer blockieren können.

skills/custom/web/scripts/with_server.py:79 - medium - Die Bereitschaftsprüfung akzeptiert jeden Listener auf dem Port und prüft nicht, ob der gestartete Prozess noch lebt, dadurch können Tests gegen einen fremden Altprozess laufen.

skills/custom/web/scripts/with_server.py:96 - medium - Bei `shell=True` wird nur der Shell-Prozess beendet, Kindprozesse können weiterlaufen und Ports sowie Ressourcen nach dem Test blockieren.

scripts/skills/update-vendor.ps1:21 - medium - `-RefreshLock` aktualisiert den Repository-Commit ohne die vendorten Inhalte oder Skill-Commits zu aktualisieren, danach meldet der Status fälschlich einen aktuellen Lock.

scripts/skills/vendor-import.ps1:12 - medium - Der Installerpfad enthält den festen Benutzer `Marcus`, wodurch der zentrale Import-Workflow auf anderen Maschinen unmittelbar abbricht.

scripts/skills/lib.ps1:192 - medium - GitHub-Aufrufe besitzen weder Timeout noch kontrollierte Fehlerbehandlung, bei Netzwerkproblemen oder Rate-Limits kann die Vendor-Pipeline unbegrenzt warten oder mit einer rohen Exception abbrechen.

scripts/skills/lib.ps1:87 - medium - Doppelte Bundle-IDs überschreiben sich still im Hashtable, der Validator sieht anschließend nur den letzten Eintrag und kann die Kollision nicht mehr erkennen.

scripts/skills/archive-report.ps1:17 - low - Ein unbekanntes Profil wird still als leeres Profil behandelt, wodurch ein plausibel aussehender, aber sachlich falscher Archivbericht entsteht.

## 2. Ideen

[Quick Win] - Bootstrap-Parameter per Hashtable-Splatting übergeben und einen Multi-Bundle-Test ergänzen - macht den Projekt-Bootstrap erstmals zuverlässig ausführbar.

[Quick Win] - `SupportsShouldProcess`, `-WhatIf` und einen vollständig nebenwirkungsfreien Dry-Run einführen - schafft eine vertrauenswürdige Vorschau vor Dateioperationen.

[Quick Win] - Skill- und Bundle-IDs per Allowlist validieren und ausschließlich `-LiteralPath` verwenden - verhindert Traversal, Wildcard-Löschungen und mehrdeutige Namen.

[Quick Win] - Profilfelder einschließlich `default_targets` und unbekannter Properties im Validator prüfen - verhindert stille Konfigurationsfehler und unerwartete Zielinstallationen.

[Mittel] - Sync über temporäre Verzeichnisse, Validierung und atomaren Austausch implementieren - erhält bei Kopierfehlern garantiert die letzte funktionierende Installation.

[Mittel] - Pester-Tests für Argumentbindung, fehlgeschlagene Native Commands, Portkonflikte, doppelte IDs und bösartige Pfade aufbauen - schützt die kritischen Edge-Cases dauerhaft vor Regressionen.

[Mittel] - Den Regex-YAML-Parser durch einen echten Parser plus Schema-Validierung ersetzen - verhindert stilles Fehlparsen legitimer YAML-Konstrukte.

[Mittel] - Server-Helper mit Prozessgruppen, geerbten Log-Streams und Prozesszustandsprüfung ausstatten - vermeidet Deadlocks, verwaiste Prozesse und falsche Readiness-Signale.

[Gross] - Vendor-Import als transaktionale, commit-genaue Pipeline mit Staging, Inhalts-Hashes und atomischem Lock-Update bauen - liefert reproduzierbare und auditierbare Drittanbieter-Skills.