# Antigravity Bundle Workflows

These files mirror bundle entry points for `~/.gemini/antigravity/global_workflows`.

Use one workflow file as session starter, then load the listed core skills first.
Add extended skills only if required by the task complexity.
