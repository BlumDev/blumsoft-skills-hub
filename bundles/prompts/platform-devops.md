Use the `Platform DevOps` bundle.

Default flow:
- `backend` for service architecture
- `deployment-procedures` for release decisions
- `docker-expert` for container work
- `observability-engineer` for production visibility

Pull extended infra skills only when the task actually reaches IaC, Kubernetes, CI/CD, tracing, or incident response.
