# billing-demo

Small internal billing & reporting service (demo fixture).

- `src/accounts.py` — account lookup, creation, password reset
- `src/reports.py` — report generation and aggregation
- `src/assistant.py` — LLM helper for support queries
